# 图片差异化生成与人工审核平台——需求规格

## 1. 文档状态

- 版本：首版需求基线
- 日期：2026-08-26
- 状态：已完成需求访谈，等待用户审核
- 目标平台：Windows 与 macOS
- 使用方式：单机、单用户、本地 Chrome 浏览器

## 2. 项目目标

为数千至数万张游戏图片建立一套可持续运行的工作流：

1. Codex 按用户指令整理和读取原图。
2. Codex 对原图进行 AI 差异化生成。
3. 审核员在本地网页中并排查看原图与修改图。
4. 审核员快速执行通过、待重做或剔除。
5. Codex 根据待重做记录生成新版本。
6. 审核通过的图片由网页统一导出到成品目录。

系统不要求一次性完成全部图片。每天生成多少、何时生成以及生成哪个批次，完全由用户控制。

## 3. 核心原则

- 审核网页只承接人工审核，不负责调用模型生图。
- 网页不进行自动质量判断，也不突出疑似问题。
- Codex 负责原图整理、差异化生图以及待重做处理。
- 不接入独立的 OpenAI 图片 API，不建立 API 自动生图队列。
- 原图只读，任何流程都不得覆盖或删除原图。
- 审核操作必须实时存档，程序异常退出后能够恢复。
- 首版保持简单，不增加多人协作、账号、权限或云端部署。

## 4. 图片差异化要求

### 4.1 允许发生的变化

- 主体允许发生明显变化。
- 角度、构图、背景、光照、色彩、装饰元素和氛围均可变化。
- 模型可以组合一至两种兼容的修改策略。
- 不要求修改图与原图保持很高的视觉相似度。

### 4.2 必须保留的特征

- 保留原图的核心主题和用途。
- 保持同一美术风格大类，例如像素画仍为像素画、二次元仍为二次元。
- 最终结果必须符合常理，适合作为游戏内容使用。

### 4.3 文字与品牌元素

- 原图中的必要文字、Logo、水印或品牌元素不得被随意篡改。
- 不需要保留时，允许删除。
- 不应让图像模型重新生成重要文字，以降低错字和乱码风险。

### 4.4 修改策略

- Codex 根据图片内容自行选择预设策略，审核员不逐张选择模板。
- 允许的策略包括但不限于：更换背景、调整光影与配色、改变氛围、替换装饰元素、轻微或中度改变构图。
- Codex 应记录每次采用的生成说明，供失败重做时参考；审核网页无需强制展示该说明。

## 5. 项目目录

每个项目使用一个独立的“项目数据”目录。首版一次只打开一个项目。

```text
项目数据/
  原图/
  修改图/
  审核通过的成品/
  .review/
    review.db
    redo_queue.json
    import_manifest.json
    settings.json
    backups/
```

对审核员可见的业务目录只有三个：`原图`、`修改图`、`审核通过的成品`。`.review` 是内部数据目录，不在审核界面中展示。

## 6. 编号与文件规则

### 6.1 文件夹规则

- 每十张原图组成一个编号文件夹。
- 文件夹使用六位自然数全局连续编号：`000001`、`000002`、`000003`……
- 最后一个文件夹允许少于十张。
- 三个业务目录保持相同的编号文件夹结构。

### 6.2 图片编号

- 图片使用六位自然数全局连续编号。
- `000001` 文件夹包含图片 `000001` 至 `000010`。
- `000002` 文件夹包含图片 `000011` 至 `000020`，依此类推。
- 三个业务目录中的同一图片使用相同的数字文件名主体。
- 扩展名可以不同；系统通过“编号文件夹 + 数字文件名主体”配对。

### 6.3 修改图版本

修改图必须增加版本后缀，避免重做时覆盖失败版本。

```text
原图/000001/000003.webp
修改图/000001/000003_v001.jpg
修改图/000001/000003_v002.jpg
审核通过的成品/000001/000003.jpg
```

- `v001` 为第一次生成。
- 重做时依次产生 `v002`、`v003`……
- 网页只把最高版本作为当前待审核图片。
- 旧版本保留在“修改图”目录，不从硬盘删除。

## 7. 原图导入与整理

原图整理由专用 Codex Skill 完成，不属于审核网页职责。

### 7.1 导入行为

- 用户向 Codex 提供原始图片所在目录。
- Codex Skill 递归查找 JPEG、PNG 和 WebP 静态图片。
- 不处理 GIF、PSD、SVG 或视频帧。
- 按原始相对路径和文件名进行自然排序。
- 将图片复制到“原图”目录，不移动、不修改源文件。
- 每十张建立一个编号文件夹并分配全局图片编号。
- 记录原始路径、内容指纹、目标编号和文件格式。

### 7.2 去重

- 通过文件内容指纹识别重复图片，不仅比较文件名。
- 重复导入同一内容时自动跳过。
- 导入结束后报告新增数量、跳过数量和无法读取数量。
- 同一批源文件重复导入时，已有图片编号保持不变。

## 8. Codex Skill

需要建立一个专用 Codex Skill，封装日常生成流程。

### 8.1 用户命令

至少支持以下自然语言命令：

- “导入这些原图”
- “生成下一组”
- “生成 000127”
- “处理待重做”
- “重做 000127 中的 001263”

### 8.2 生成下一组

- 自动寻找尚未生成修改图的最小编号文件夹。
- 每次默认处理该文件夹中的最多十张图片。
- 根据每张图的核心主题和美术风格自行选择差异化策略。
- 生成结果写入对应的“修改图”编号文件夹。
- 初次生成使用 `_v001`；不得覆盖已有文件。
- 用户控制何时开始下一组，Skill 不设每日产量或完成标准。

### 8.3 处理待重做

- 读取内部 `.review/redo_queue.json`。
- 获取原图、当前失败版本和审核原因。
- 根据失败原因调整生成策略。
- 写入下一个版本号。
- 不删除旧版本。
- 新文件完整写入后，审核网页自动把旧项移出待重做页面，并把新版本加入待审核队列。

### 8.4 Codex 与网页的边界

- 网页不得尝试自动唤起或控制 Codex。
- Codex 不直接修改审核数据库中的人工结论。
- 双方通过约定目录、文件名和内部 JSON 文件交接。
- `redo_queue.json` 不在网页中展示，审核员无需手动读取或编辑。

## 9. 审核网页

### 9.1 页面范围

首版只提供两个业务页面：

1. 主审核页：顺序显示尚未审核的最新修改图。
2. 待重做页：显示被标记为待重做且尚未产生新版本的图片。

不提供通用状态筛选、复杂搜索或编号跳转。

### 9.2 主审核页布局

- 原图在左，修改图在右，各占可用区域的 50%。
- 默认将两张图完整适配到窗口。
- 两侧同步缩放、同步拖动。
- 双击恢复适应窗口。
- 不提供闪切模式。
- 不自动标注或突出任何问题。
- 操作完成后自动进入下一张未审核图片。
- 重启后自动恢复到第一张未审核图片。

### 9.3 快捷操作

- `A`：通过并进入下一张。
- `R`：标记为待重做，选择原因后进入下一张。
- `X`：剔除并进入下一张。
- `←`：上一张。
- `→`：下一张。
- `Z` 或 `Ctrl/Cmd + Z`：撤销上一次审核操作。

鼠标主要用于图片缩放、拖动和点击按钮。所有核心审核动作都必须能通过键盘完成。

### 9.4 待重做原因

按 `R` 后出现快速原因选择器，可使用数字键选择，也允许跳过。

首版默认原因：

- 主体或结构不合理
- 风格不符
- 文字或 Logo 错误
- 构图不佳
- 明显生成瑕疵
- 与原图差异不足

原因列表保存在本地设置中，允许用户自行增删，无需修改代码。

### 9.5 剔除

- 剔除表示该原图退出生成和审核流程。
- 剔除项不进入待重做页面。
- 文件不从硬盘删除。
- 在撤销仍可用的范围内允许恢复。

## 10. 状态流转

```text
未生成
  → Codex 生成
待审核
  ├─ 通过 → 已通过、待导出
  ├─ 待重做 → 待重做页面
  │              → Codex 生成新版本
  │              → 旧版本退出待重做页面
  │              → 新版本进入待审核
  └─ 剔除 → 流程结束

已通过、待导出
  → 点击导出
  → 已导出并从网页待处理数据中清除
```

## 11. 导出

- 主审核页提供“导出已通过图片”按钮。
- 点击后，将所有尚未导出的通过项复制到“审核通过的成品”目录。
- 成品去掉版本后缀，例如 `000003_v002.jpg` 导出为 `000003.jpg`。
- 直接复制审核时看到的修改图，不再次压缩或转换。
- 成功导出后，将记录标记为已导出并从网页待处理数据中清除，防止重复导出。
- 如果成品目录已有同编号旧成品，允许使用新通过版本原子替换。
- 旧生成版本仍保存在“修改图”目录，因此替换成品不会丢失生成历史。
- 只有文件复制全部成功后，才能更新“已导出”状态。
- 复制失败时保留网页记录，并显示可重试错误。

## 12. 存档与数据一致性

### 12.1 IndexedDB

- 每次审核按键后立即写入 Chrome IndexedDB。
- 用于界面即时恢复和低延迟交互。
- IndexedDB 不保存不必要的完整图片副本，只保存界面状态和待同步操作。

### 12.2 SQLite

- 本地后台同时将审核结果写入 `.review/review.db`。
- SQLite 是权威记录。
- IndexedDB 与 SQLite 不一致时，以 SQLite 为准，并自动修复 IndexedDB。
- 每个操作应具备唯一标识，重复提交不能产生重复状态变化。

### 12.3 自动备份

- 每次启动时自动备份一次 SQLite 数据库。
- 仅保留最近十份备份。
- 备份不复制图片文件。
- 数据库损坏时允许从最近备份恢复。

## 13. 新版本检测

- 本地后台持续或定时扫描“修改图”目录。
- 只接收符合编号与版本规则且已完整写入的文件。
- 对同一图片选择最高版本号作为最新版本。
- 待重做图片出现更高版本后：
  1. 旧版本从待重做列表中移除；
  2. 旧文件保留在硬盘；
  3. 新版本状态设为待审核；
  4. 新版本加入主审核队列。
- 临时文件、写入中的文件和不符合命名规则的文件不得加入队列。

## 14. 启动与兼容性

- Windows 和 macOS 均应提供可双击的启动入口。
- 启动后自动打开 Chrome。
- 不要求用户每次在终端输入命令。
- 固定使用 `http://127.0.0.1:4173`，确保 IndexedDB 来源稳定。
- 端口被占用时明确提示，不静默切换端口。
- 本地服务只绑定 `127.0.0.1`，不向局域网开放。
- 首版仅保证 Chrome，不保证 Safari、Firefox 或其他浏览器。
- 首次启动选择一个项目数据目录，之后自动重新打开最近项目。
- 可以切换项目，但同一时间只打开一个项目。

## 15. 异常恢复与安全要求

- 原图目录始终按只读方式处理。
- 所有图片先写入临时文件，成功完成后再原子改名。
- 程序崩溃后不得丢失已经完成的审核操作。
- 中断的导出不得错误标记为已导出。
- 不得因网页刷新、Chrome 重启或 IndexedDB 清理而丢失 SQLite 中的权威记录。
- 不得静默覆盖修改图历史版本。
- 文件缺失、损坏或无法解码时显示明确错误，但不自动作出质量结论。

## 16. 首版明确不做

- 网页直接调用 Codex 或任何图片 API。
- 自动图片质量审核、相似度评分或问题高亮。
- 每日产量、生成速度或通过率目标。
- 一次性自动生成全部图片。
- 多用户、账号、权限和云同步。
- 云端部署或公开访问。
- GIF、PSD、SVG、视频处理。
- 通用搜索、复杂筛选和统计后台。
- 原图与修改图闪切对比。
- Safari 和 Firefox 兼容。

## 17. 首版验收标准

### 17.1 导入与编号

- 能从散乱源目录复制并整理至少 100 张测试图片。
- 每十张进入一个六位编号文件夹，最后一组可不足十张。
- 重复导入不会产生重复图片或改变已有编号。

### 17.2 Codex 生成交接

- “生成下一组”能够找到正确编号文件夹并生成对应 `_v001` 文件。
- “处理待重做”能够读取内部重做清单并产生递增版本号。
- 任何生成操作都不会覆盖原图或旧修改图。

### 17.3 审核效率

- 主审核页能稳定并排显示原图与最新修改图。
- 同步缩放、同步拖动和快捷键操作正常。
- 审核后自动进入下一张，刷新或重启后可继续。
- 待重做页面只显示当前仍需重做的图片。

### 17.4 状态与恢复

- 每次审核操作立即写入 IndexedDB，并可靠同步至 SQLite。
- 重启后状态与关闭前一致。
- 撤销不会造成数据库与界面状态分叉。
- 新修改版本出现后能正确回到待审核队列。

### 17.5 导出

- 点击导出后，所有未导出的通过项进入正确的成品编号文件夹。
- 成品内容与审核通过的文件完全一致。
- 导出成功后不再重复显示或重复导出。
- 导出失败时不丢失记录，可以再次尝试。

## 18. 推荐实施顺序

1. 实现目录、编号、数据库和状态模型。
2. 实现原图导入整理脚本。
3. 实现主审核页、左右并排查看和快捷键。
4. 实现待重做页面与内部 JSON 交接。
5. 实现导出、撤销、IndexedDB 同步和数据库备份。
6. 创建 Codex Skill，接入导入、生成下一组和处理待重做流程。
7. 在 Windows 上完成测试，再在 macOS Chrome 上验证启动、路径和快捷键。

## 19. 开发前仍需通过样本验证的事项

以下项目不阻塞首版架构，但必须在取得真实图片后用样本确认：

- 图片类型分布、常见尺寸、透明通道和色彩配置。
- Codex 对不同美术风格的实际生成效果。
- 单张生成耗时及当前账户使用限制。
- 最有效的差异化提示模板。
- 实际失败原因列表是否需要调整。
- 超大图片在 Chrome 中的内存占用和缩放流畅度。

